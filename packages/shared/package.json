{
	"name": "@flerovium/shared",
	"version": "0.0.1",
	"description": "Shared components",
	"author": "David",
	"homepage": "",
	"license": "MIT",
	"private": true,
	"main": "lib/index.js",
	"types": "lib/index.d.ts",
	"directories": {
		"lib": "lib",
		"src": "src",
		"test": "__tests__"
	},
	"files": [
		"lib",
		"src"
	],
	"scripts": {
		"build": "rollup -c rollup.config.js",
		"build:prod": "rimraf lib && npm run eslint:fix && npm run prettier:format && npm run build",
		"prettier:format": "prettier --write src",
		"prettier:lint": "prettier --check src",
		"eslint:fix": "eslint --fix src",
		"eslint:lint": "eslint src",
		"test:web": "web-test-runner",
		"test:web:watch": "web-test-runner --watch",
		"test:jest": "jest",
		"test:jest:watch": "jest --watch"
	},
	"dependencies": {
		"argon2": "^0.28.1",
		"argon2-browser": "^1.18.0",
		"buffer": "6.0.3"
	},
	"devDependencies": {
		"@esm-bundle/chai": "^4.3.4",
		"@types/argon2-browser": "^1.12.0"
	}
}